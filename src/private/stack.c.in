// Auto-generated, don't edit.
// Copyright (c) 2017-2018 Nguyen Viet Giang. All rights reserved.
#include "${TYPE_SIMPLE_NAME}_stack.h"
#include "../utils.h"

aresult_t
a${TYPE_SIMPLE_NAME}_stack_realloc(
    a${TYPE_SIMPLE_NAME}_stack_t *s, u32 capacity)
{
    const u32 new_cap = apowof2_ceil(capacity);
    if (new_cap != 0) {
        void *const items = AMAKE_ARRAY(
            s->a, ${TYPE_NAME}, new_cap);
        if (!items) return AR_MEMORY;
        s->count = AMIN(s->count, s->capacity);
        memcpy(items, s->items, sizeof(${TYPE_NAME})*s->count);
        AFREE(s->a, s->items);
        s->items = (${TYPE_NAME}*)items;
        s->capacity = new_cap;
    }
    return AR_SUCCESS;
}